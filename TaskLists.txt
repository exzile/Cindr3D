## DesignCAD - Active Implementation Task List
## Status key: [~] partial  [ ] open  [>] in progress  [skip] intentional no-op / deferred spike
## Last updated: 2026-06-02

Purpose:
Keep this file focused on active work. Open task sections retain their detailed instructions.
Completed sections are removed after shipping; see git history if detail is needed.

# ══════════════════════════════════════════════════════════════════════════════
# PERF: Viewport performance sweep
# ══════════════════════════════════════════════════════════════════════════════

[x] SketchDimensionAnnotations PERF refactor complete (2026-06-02).
    Memo now depends on [activeSketch?.id, activeSketch?.dimensions] — doesn't rebuild
    when only entity positions change during constraint-solving drag. Position updates
    move to useFrame via writePairsToSegments() which writes into pre-allocated
    Float32Array (no GPU buffer allocation). All computation extracted to
    sketchDimensions/dimensionPairComputer.ts (pure function, no THREE allocs).

# ══════════════════════════════════════════════════════════════════════════════
# SURF: Surface workspace functional quality gaps
# ══════════════════════════════════════════════════════════════════════════════
# TOOL-PRESENCE PARITY IS COMPLETE. All SURF-CREATE items shipped 2026-06-02.
# Verification gate: npx tsc --noEmit clean · npx vitest run green · manual QA.

[~] SURF-4  Surface Fillet on OCC open-shell edges — code ready, manual QA pending.
    Code path fully cleared (2026-06-02):
    - isOccBodySolid guard removed from edgeModApply.ts (was incorrectly blocking shells).
    - BRepFilletAPI_MakeFillet works on shells per OCC design.
    - Dry-run short-circuit (added 2026-06-02) prevents any freeze: probe runs ONE Build(),
      returns clean error if OCC can't solve the topology — no cascade, no hang.
    - WASM exception leak fixed (freeWasmException centralized) so failed builds don't grow heap.
    Remaining: manual browser QA only — open Surface tab, Revolve a line in surface mode
    to create an OCC surface body, open Fillet, pick an edge at a small radius (≤1mm),
    verify healthy result OR a clear error message (not a freeze). Capture [occFillet]
    console lines if OCC fails on a specific topology.

[x] SURF-5  True G1/G2 face-tangency for Fill — complete (2026-06-02).
    FillEdgePicker now finds the adjacent face ptr at pick time via TopExp_Explorer_2.
    commitFill resolves both the TopoDS_Edge VIEW (from body.edgeIds) and the
    TopoDS_Face VIEW (from body.faceIds + adjacentFacePtr). MakeFilling.Add_3
    (edge, face, order, isBound) is called for G1/G2 edges; Add_2 fallback for G0.
    fillSurface.ts extended with Add_3 type. analysisState fillBoundaryEdgeData
    extended with adjacentFacePtr.

# Also fixed 2026-06-02: Fill shows status message instead of silent fallback quad
# when no edges are picked; Surface Merge uses feature-ID lookup instead of fragile
# userData.faceId; revolveActions.ts mojibake cleaned up.
