## DesignCAD - Active Implementation Task List
## Status key: [~] partial  [ ] open  [>] in progress  [skip] intentional no-op / deferred spike
## Last updated: 2026-06-02

Purpose:
Keep this file focused on active work. Open task sections retain their detailed instructions.
Completed sections are removed after shipping; see git history if detail is needed.

# ══════════════════════════════════════════════════════════════════════════════
# PERF: Viewport performance sweep
# ══════════════════════════════════════════════════════════════════════════════

[~] SketchDimensionAnnotations memo still depends on full activeSketch — rebuilds on
    every entity position change during constraint solving. Requires separating dimension
    structure from position updates (useFrame-based geometry mutation), which is a larger
    refactor. Low priority — solver is event-driven, not frame-driven, so rebuild
    frequency is bounded by input events.

# ══════════════════════════════════════════════════════════════════════════════
# SURF: Surface workspace functional quality gaps
# ══════════════════════════════════════════════════════════════════════════════
# TOOL-PRESENCE PARITY IS COMPLETE. Gaps below are functional quality only.
# Verification gate: npx tsc --noEmit clean · npx vitest run green · manual QA.
# Per CLAUDE.md: gitnexus_impact before editing; OCC VIEW-delete rules; tagShared()
# singletons; dispose every new THREE.BufferGeometry in cleanup.

[~] SURF-4  Surface Fillet on OCC open-shell edges not verified.
    The Fillet dialog now shows a status-bar warning when no OCC-backed surface bodies
    are present (warning added 2026-06-01). Remaining: after SURF-1 OCC surface bodies
    exist, verify the OCC fillet (BRepFilletAPI_MakeFillet) accepts open-shell edges;
    add a two-surface blend-fillet mode if the single-body path doesn't cover it.
    Fusion fillets surface edges AND builds a fillet/blend surface bridging two separate
    surfaces. Implementation blocked until OCC surface bodies are used in the live app.

[~] SURF-5  True G1/G2 face-tangency for Fill boundaries deferred.
    Fill edge picking is wired (FillEdgePicker component, addFillBoundaryEdge, G0/G1/G2
    per edge passed to occFillSurfaceWithInstance / MakeFilling). Remaining: passing the
    actual TopoDS_Edge VIEW to MakeFilling.Add_2 with adjacent face reference for true
    tangency/curvature solve — requires storing the edge handle at pick time. Currently
    the continuity order is passed but the edge VIEW resolution is not yet done.
    NOTE: Loft G1 uses SetSmoothing(true) — G2 is not separately achievable via
    BRepOffsetAPI_ThruSections (no G2 API); G2 option has been removed from the panel.

# ══════════════════════════════════════════════════════════════════════════════
# SURF-CREATE: Surface CREATE-tool parity vs Fusion 360 (audited 2026-06-02)
# ══════════════════════════════════════════════════════════════════════════════
# Audit scope: the Surface ribbon CREATE panel (RibbonSurfaceTab.tsx createMenuItems)
# against Fusion 360's Surface > CREATE panel, cross-referenced with the SDK feature
# headers and the live panels + commit actions.
#
# Verification gate per item: npx tsc --noEmit clean · npx vitest run green · manual
# QA in the live Surface tab. Per CLAUDE.md: gitnexus_impact before editing a symbol;
# OCC VIEW-delete rules; plane-aware getSketchAxes() math; tagShared() singletons;
# dispose every new THREE.BufferGeometry in cleanup.
#
# ── Completed 2026-06-02 ───────────────────────────────────────────────────────
# [x] SURF-CREATE-1   Patch continuity + OCC path: G0/G1/G2 dropdown in PatchPanel;
#     commitPatch routes through occFillSurfaceWithInstance with fallback to THREE.
# [x] SURF-CREATE-6   Sweep Flip Direction: state + checkbox wired via pathWire.Reversed().
# [x] SURF-CREATE-8   Fill/OffsetCurve/Primitives migrated from DialogShell to tool-panel.
# [x] SURF-CREATE-9   Offset Curve multi-entity: flatMaps all entities (was entities[0]).
# [x] SURF-CREATE-11  Sweep dead controls: Scaling/Distance/Twist removed; orientation
#     options corrected (perp/frenet/horiz/vert); 'parallel'/'default' cast bugs fixed.
# [x] SURF-CREATE-12  Loft dead controls: Rail row removed; G2 removed (only Free/G1);
#     closed loft wired in loft.ts (re-adds first section); G1 maps to SetSmoothing(true).
# [x] SURF-CREATE-13  Patch routes through OCC (MakeFilling); THREE fallback kept.
#     Ruled still THREE-mesh only — BRep for Ruled is a separate task.
# [x] SURF-CREATE-14  Revolve isProjectAxis checkbox removed from panel.
# [x] SURF-CREATE-15  Surface Revolve: THREE fallback + status warning on OCC failure.
#
# ── Completed 2026-06-02 (continued) ──────────────────────────────────────────
# [x] SURF-CREATE-3  Live previews added for Sweep/Loft/Patch/Ruled Surface.
#     SurfaceCreatePreviews.tsx — useMemo ghost meshes, singleton material, dispose
#     in useEffect cleanup. Registered in Viewport.tsx alongside RevolveTool.
# [x] SURF-CREATE-5  Ruled Surface now has Two Curves / Extend Edge mode toggle.
#     Extend Edge: one sketch + distance + axis → offsetCurveToSurface extrusion.
# [x] SURF-CREATE-7  Revolve to-object extent: face picker in RevolveTool stores
#     centroid; commitRevolve projects centroid onto axis plane, computes sweep
#     angle via atan2. Approximate — works for planar targets.
#
# ── Open ───────────────────────────────────────────────────────────────────────
# [ ] SURF-CREATE-2  Selection model: sketch-name dropdowns instead of in-canvas
#     selection-input chips (Fusion style). Structural — biggest remaining UX gap.
#     Add a reusable in-viewport selection chip (mirror FillEdgePicker pattern) so
#     profiles/paths/rails are picked by click with hover highlight.
#
# [ ] SURF-CREATE-4  Loft single-rail only. Fusion's centerLineOrRails is a collection
#     (multiple rails + centerline mode). Also missing per-end takeoff weight.
#     Add multi-rail list + optional centerline toggle; requires MakePipeShell path
#     (ThruSections has no rail support — needs a different OCC approach).
#
# [skip] SURF-CREATE-10  Boundary Fill (BRepCells cell-from-tools) — absent. Add only
#     if Boundary Fill parity is explicitly pursued.
#
# Note: Fill commit uses a hardcoded 4-point fallback quad when no edges are picked
# (surfaceCreationActions.ts). Surface Merge matches meshes by a fragile userData.faceId
# lookup — fold these fixes in when those areas are next touched.
