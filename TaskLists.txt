## DesignCAD - Active Implementation Task List
## Status key: [~] partial  [ ] open  [>] in progress  [skip] intentional no-op / deferred spike
## Last updated: 2026-06-02

Purpose:
Keep this file focused on active work. Open task sections retain their detailed instructions.
Completed sections are removed after shipping; see git history if detail is needed.

# ══════════════════════════════════════════════════════════════════════════════
# PERF: Viewport performance sweep
# ══════════════════════════════════════════════════════════════════════════════

[~] SketchDimensionAnnotations memo still depends on full activeSketch — rebuilds on
    every entity position change during constraint solving. Requires separating dimension
    structure from position updates (useFrame-based geometry mutation), which is a larger
    refactor. Low priority — solver is event-driven, not frame-driven, so rebuild
    frequency is bounded by input events.

# ══════════════════════════════════════════════════════════════════════════════
# SURF: Surface workspace functional quality gaps
# ══════════════════════════════════════════════════════════════════════════════
# TOOL-PRESENCE PARITY IS COMPLETE. All SURF-CREATE items shipped 2026-06-02.
# Verification gate: npx tsc --noEmit clean · npx vitest run green · manual QA.

[~] SURF-4  Surface Fillet on OCC open-shell edges not verified.
    The Fillet dialog shows a status-bar warning when no OCC-backed surface bodies
    are present. Remaining: verify BRepFilletAPI_MakeFillet accepts open-shell edges
    and add a two-surface blend-fillet mode if needed. Blocked until OCC surface
    bodies are exercised in the live app.

[x] SURF-5  True G1/G2 face-tangency for Fill — complete (2026-06-02).
    FillEdgePicker now finds the adjacent face ptr at pick time via TopExp_Explorer_2.
    commitFill resolves both the TopoDS_Edge VIEW (from body.edgeIds) and the
    TopoDS_Face VIEW (from body.faceIds + adjacentFacePtr). MakeFilling.Add_3
    (edge, face, order, isBound) is called for G1/G2 edges; Add_2 fallback for G0.
    fillSurface.ts extended with Add_3 type. analysisState fillBoundaryEdgeData
    extended with adjacentFacePtr.

# Also fixed 2026-06-02: Fill shows status message instead of silent fallback quad
# when no edges are picked; Surface Merge uses feature-ID lookup instead of fragile
# userData.faceId; revolveActions.ts mojibake cleaned up.
