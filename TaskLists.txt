## DesignCAD — Implementation Task List
## Status key: [x] done  [~] partial (wired, incomplete)  [ ] not started
## Last updated: 2026-05-23  Branch: codex/occ-only-remove-fallbacks
## SDK ref: C:\Users\joeyp\AppData\Local\Autodesk\webdeploy\production\
##          4bca736941837d3e42bba21bb36b9891e34b2fce\CPP\include\Fusion\
## Fusion 360 parity audit: 2026-05-23 (see OCC-10 phase)


# ══════════════════════════════════════════════════════════════════════════════
# OPENCASCADE WASM MIGRATION — Replace mesh-CSG with BRep kernel
# WHY: mesh-CSG + earcut cannot produce CAD-quality fillets (wing artifact
# is mathematically inherent). OCC produces exact toroidal surfaces.
# GATE: removed. OCC is now the only edge-modification path.
# ══════════════════════════════════════════════════════════════════════════════

# ── PHASE OCC-1: Foundation ────────────────────────────────────────────────────
[x] OCC-1.1 — WASM build selection (package.json dep, vite config, wasm chunk)
[x] OCC-1.2 — Loader scaffold (getOcc/getOccSync singleton, src/engine/occ/loader.ts)
[x] OCC-1.3 — OccHandle wrapper + FinalizationRegistry auto-delete (occHandle.ts)
[x] OCC-1.4 — Hello-world spike (box + tessellate + render, OccSpikePanel)

# ── PHASE OCC-2: BRep data model ───────────────────────────────────────────────
[x] OCC-2.1 — BRepBody interface (shape OccHandle, faceIds/edgeIds/vertexIds Maps)
[x] OCC-2.2 — STEP serializer (shapeToStep / shapeFromStep via Emscripten FS)
[x] OCC-2.3 — Tessellator (BRepTessellation: positions/normals/faceIds/edgePolylines)
[x] OCC-2.4 — Re-tessellate-on-zoom hook (linearDeflection scales with camera distance)

# ── PHASE OCC-3: Primitives + core ops ────────────────────────────────────────
[x] OCC-3.1 — Box primitive (BRepPrimAPI_MakeBox_2, optional transform)
[x] OCC-3.2 — Cylinder primitive (BRepPrimAPI_MakeCylinder_2, 3 face IDs)
[~] OCC-3.3 — Sketch-based extrude
#     Status: sketchToWire + BRepPrimAPI_MakePrism contract test passes.
#     Remaining: full feature-tree wiring + taper angle (see OCC-10.3).
[~] OCC-3.4 — Revolve
#     Status: BRepPrimAPI_MakeRevol contract test passes.
#     Remaining: feature-tree wiring + two-sided/to-entity extents (see OCC-10.13).
[~] OCC-3.5 — Sweep (basic profile+path via BRepOffsetAPI_MakePipe)
#     Status: contract test passes.
#     Remaining: feature-tree wiring + guide rails/taper/twist (see OCC-10.4).
[x] OCC-3.6 — Shell (BRepOffsetAPI_MakeThickSolid)
#     Engine + feature tree + UI: all done (see OCC-10.9).

# ── PHASE OCC-4: Booleans ──────────────────────────────────────────────────────
[x] OCC-4.1 — Subtract (BRepAlgoAPI_Cut_3)
[x] OCC-4.2 — Union (BRepAlgoAPI_Fuse_3)
[x] OCC-4.3 — Intersect (BRepAlgoAPI_Common_3)

# ── PHASE OCC-5: Fillet & Chamfer ─────────────────────────────────────────────
[x] OCC-5.1 — Fillet (constant Add_2, variable Add_3, chord-length, G2 ChFi3d_Polynomial,
#              multi-set OccFilletEdgeSet collection; featureMeshActions.ts wired)
[x] OCC-5.2 — Chamfer (equal-distance, two-distance; featureMeshActions.ts wired)
[x] OCC-5.3 — Bracket-boss verification & migration cutover (flag default ON)

# ── PHASE OCC-6: Sketch → BRep face ───────────────────────────────────────────
[x] OCC-6.1 — Sketch segments to TopoDS_Edge (line/arc/circle/ellipse/spline)
[x] OCC-6.2 — Closed loop to TopoDS_Wire (normalize, degenerate detection, auto-close)
#     Implemented: sketchEntitiesToWire in src/engine/occ/sketchEntityToWire.ts
[x] OCC-6.3 — Region with holes to TopoDS_Face (hole orientation via BRepBuilderAPI_MakeFace)
#     Implemented: wiresToFace in src/engine/occ/sketchEntityToWire.ts
[x] OCC-6.4 — sketchPlaneFromFace (BRep_Tool.Surface → SketchPlane for planar faces)

# ── PHASE OCC-7: Feature tree wiring ──────────────────────────────────────────
[x] OCC-7.1 — BodyDef.shape replaces BodyDef.mesh; useBodyMesh lazy tessellation selector
[x] OCC-7.2 — Feature evaluation pipeline (featureEvaluator cache, cascade invalidation)
[x] OCC-7.3 — Undo / STEP snapshots (captureOccSnapshot / restoreOccSnapshot)
[x] OCC-7.4 — Persistence in .exzile v2 files (stepString blobs, backward compat v1)

# ── PHASE OCC-8: Selection migration ──────────────────────────────────────────
[x] OCC-8.1 — Face picker (BRepTessellation.faceIds triangle lookup, OccFacePicker)
[x] OCC-8.2 — Edge picker (edgePolylines LineSegments, edgeId raycast)
[x] OCC-8.3 — Sketch-plane picker (HoleFacePicker OCC path, planar face validation)

# ── PHASE OCC-9: Cleanup (OCC-only branch) ──
[x] OCC-9.1 — Delete mesh-CSG edge-cut entry points
#     rm src/utils/geometry/edgeCutCore.ts
#     rm src/utils/geometry/filletGeometry.ts
#     rm src/utils/geometry/chamferGeometry.ts
#     rm src/store/cad/slices/featureManagement/applyEdgeCut.ts
#     rm src/store/cad/slices/featureManagement/resolveBodySource.ts
[x] OCC-9.1b — Delete live edge-cut preview helpers
#     rm src/components/viewport/scene/{FilletPreview,ChamferPreview}.tsx
#     rm src/components/viewport/scene/edgeOp/{EdgeOpPreview,edgeOpGuideOverlay,edgeOpHighlightIds,previewScene,useDebouncedEdgeIds}.*
#     rm src/workers/edgeOpWorker.ts
[ ] OCC-9.2 — Delete weld/CSG plumbing [BLOCKED]
#     2026-05-23 audit: csg.ts imported by 11 files (csgWorker, GeometryEngine, ExtrudePreview,
#     snapFit, lipGroove, plateGeometryOps, etc.). weldClean/extrusion/revolve/pipe/profileSweeps
#     all imported by GeometryEngine.ts + other live code. Cannot delete until extrude/revolve/
#     sweep/loft commit actions have OCC paths that bypass the mesh-CSG pipeline entirely.
#     Blocked by: OCC-10 feature-tree wiring for extrude, revolve, sweep, loft.
[ ] OCC-9.3 — Delete edge topology extraction (edgeTopology.ts) [BLOCKED]
#     2026-05-23 audit: edgeTopology.ts imported by 14 files including ExtrudedBodies.tsx,
#     nearestEdge.ts, persistence.ts, bodyBoolean.ts, shellSolid.ts, cutTopology.ts, and
#     multiple solid ops. Not deletable until all dependents are migrated to OCC topology.
[~] OCC-9.4 — Drop deps (manifold-3d, three-bvh-csg); useOccPipeline flag removed
#     Remaining: drop mesh CSG deps only after non-edge mesh operations have OCC replacements. npm install; verify bundle shrinks ~40MB.
[~] OCC-9.5 — Memory & docs update (project_designcad.md, wasm_patterns.md)
[~] OCC-9.6 — Final dead-code audit (rg/import checks, eslint, typecheck, GitNexus
#     detect-changes; remove transitional hooks, old flag branches, debug panels)


# ══════════════════════════════════════════════════════════════════════════════
# PHASE OCC-10: Fusion 360 Feature Parity
# Source: 2026-05-23 audit against CPP/include/Fusion/ SDK headers
# All items are independent unless noted. Ordered by priority (high → low).
# ══════════════════════════════════════════════════════════════════════════════

# ── HIGH PRIORITY ──────────────────────────────────────────────────────────────

[x] OCC-10.1 — Loft (~16h)
#     Engine + feature tree + UI: all done. LoftPanel.tsx (viewport tool panel)
#     in src/components/viewport/panels/ handles profile/guide-rail selection,
#     start/end conditions, closed/smoothing options, operation type.
#     commitLoft OCC path wired (occLoftWithInstance, tessellate, attach brepBodyId).

[x] OCC-10.2 — Mirror (~8h)
#     Engine + feature tree + UI: all done. MirrorDialog.tsx in src/components/dialogs/pattern/
#     supports features/bodies/components, XY/XZ/YZ plane selection, and compute type.
#     commitMirrorFeature OCC path wired — detects brepBodyId, calls occMirrorWithInstance.

[~] OCC-10.3 — Extrude: taper angle (~6h)
#     Status 2026-05-23: Engine op done. OccExtrudeOptions.taperAngle added;
#     BRepOffsetAPI_DraftAngle_1 applied to lateral faces after prism build;
#     neutral plane = sketch plane; graceful fallback if Build fails.
#     Remaining: wire taperAngle through extrudeCommitActions OCC path (no OCC
#     path exists yet for the commit action — standard extrudes go through mesh-CSG
#     pipeline); add taper field to extrudePanel UI (ExtrudePanel.tsx already has
#     extrudeTaperAngle state, so field may already be present).

[~] OCC-10.4 — Sweep: guide rails + orientation modes (~12h)
#     Status 2026-05-23: Engine op done. OccSweepOptions extended with guideRail,
#     guideRailFrame, orientation (perpendicular/frenet/horizontal/vertical).
#     occSweepWithInstance branches: simple path (BRepOffsetAPI_MakePipe_1) when no
#     advanced options; advanced path (BRepOffsetAPI_MakePipeShell_1) when guide rail
#     or non-perpendicular orientation requested. Guide rail added via Add_2(wire,
#     true, true) for tangent-contact scaling. Orientation via SetMode_2/SetMode_3.
#     Remaining: feature-tree wiring (sweep commit action has no OCC path yet);
#     UI sweepPanel guide-rail picker + orientation dropdown;
#     taperAngle/twistAngle (complex — need per-segment transform or separate pass).

# ── MEDIUM PRIORITY ────────────────────────────────────────────────────────────

[x] OCC-10.5 — Rectangular pattern (~8h)
#     Engine + feature tree + UI: all done. RectangularPatternDialog + LinearPatternDialog
#     in src/components/dialogs/pattern/ registered in ActiveDialog.tsx.

[x] OCC-10.6 — Circular pattern (~6h)
#     Engine + feature tree + UI: all done. CircularPatternDialog in
#     src/components/dialogs/pattern/ registered in ActiveDialog.tsx.

[x] OCC-10.7 — Offset faces (press-pull) (~6h)
#     Engine + feature tree + UI: all done. OffsetFaceDialog.tsx provides distance control.
#     commitOffsetFace OCC path calls occOffsetFacesWithInstance with exact faceIds.

[x] OCC-10.8 — Draft (~8h)
#     Engine + feature tree + UI: all done. DraftDialog.tsx in src/components/dialogs/solid/
#     provides angle, pull direction, and face picker. commitDraft OCC path calls
#     occDraftWithInstance with exact faceIds + pull direction + neutral plane.

[x] OCC-10.9 — Shell: outside thickness + shell type (~4h)
#     Engine + feature tree + UI: all done. ShellDialog.tsx has direction (inside/outside/both),
#     insideThickness, outsideThickness, shellType (sharp/rounded), tangent chain, and
#     individual face offsets (SOL-I7). commitShell OCC path wired in advancedSolidAndMeshOpsSlice.

[x] OCC-10.10 — Chamfer: distance-and-angle mode (~4h)
#     Already wired: resolveOccChamferDistances in featureMeshActions.ts converts
#     mode='dist-angle' → distance2 = distance * tan(angle), passed to occChamferWithInstance
#     which uses Add_3(d1, d2, edge, refFace). Corner types have no OCC equivalent (UI-only).

[x] OCC-10.11 — Combine: multiple tool bodies (~4h)
#     Engine + feature tree + UI: all done. commitCombine accepts multiple tool IDs,
#     uses performOccBooleanMultiWithInstance with OCC BOPAlgo_BOP batch booleans.
#     CombineDialog has multi-select tool picker. No pairwise fallback remains here.

[~] OCC-10.12 — Extrude: two-sided extents (~8h)
#     Engine: extrude.ts extended — twoSideDist option extrudes in negative direction
#     and fuses via BRepAlgoAPI_Fuse_3. symmetric and twoSideDist are mutually exclusive.
#     Remaining: through-all (feature-tree concern — needs target bbox); to-entity; UI picker.

[~] OCC-10.13 — Revolve: two-sided extents (~4h)
#     Engine: revolve.ts extended — side2AngleRad option revolves in negative direction
#     and fuses via BRepAlgoAPI_Fuse_3. to-entity not yet implemented.
#     Remaining: UI extent picker + feature tree wiring.

[x] OCC-10.14 — Thicken surface bodies (~8h)
#     Engine + feature tree + UI: all done. ThickenDialog.tsx in
#     src/components/dialogs/surface/ has thickness, direction (inside/outside/symmetric),
#     operation (new-body/join/cut). Registered as 'thicken' in ActiveDialog.tsx.

# ── LOW PRIORITY ────────────────────────────────────────────────────────────────

[x] OCC-10.15 — Sphere + Torus primitives (~4h)
#     Engine: src/engine/occ/ops/sphere.ts DONE (BRepPrimAPI_MakeSphere_2, optional transform).
#             src/engine/occ/ops/torus.ts DONE (BRepPrimAPI_MakeTorus_2, majorRadius > minorRadius guard).
#     Remaining: UI primitives toolbar buttons + feature tree wiring.
#     2026-05-23: addPrimitive now creates OCC sphere/torus bodies, tessellates
#     them into stored meshes, attaches brepBodyId, and uses the existing
#     Primitive dialog/toolbar flow.

[x] OCC-10.16 — Scale (uniform + non-uniform) (~4h)
#     Engine + feature tree + UI: all done. ScaleDialog.tsx has uniform/non-uniform
#     modes, per-axis factors, reference point (centroid/origin). commitScale OCC path
#     wired in featureMeshActions.ts.

[~] OCC-10.17 — Fillet: full-round mode (~8h)
#     Status 2026-05-23: Engine op done. occFullRoundFilletWithInstance added to
#     fillet.ts. Takes centerFaceId + sideFaceIds[2] from body.faceIds map.
#     Uses TopTools_IndexedMapOfShape + TopExp.MapShapes_1 for canonical edge indexing
#     (ptr comparison is unreliable due to orientation wrappers; FindIndex_1 uses IsSame).
#     Finds shared boundary edges via center∩side1 and center∩side2 index intersection.
#     Auto-radius = half midpoint distance between the two boundary edges.
#     Falls back to BRepFilletAPI_MakeFillet_2 (same as constant fillet) — OCC resolves
#     the full-round blend when both boundary edges are filleted with the spanning radius.
#     Remaining: feature-tree wiring (fillet commit path, detect full-round mode);
#     UI face picker in filletDialog for center + side faces.

[x] OCC-10.18 — Fillet: asymmetric mode (~6h)
#     Status 2026-05-23: DONE. resolveOccFilletEdgeSets asymmetric branch now maps
#     offsetOne → startRadius, offsetTwo → endRadius → OCC Add_3(r1, r2, edge).
#     Multi-set loop also handles type='asymmetric' with the same mapping.
#     Removes the console.warn fallback. Limitation: Add_3 varies radius along edge
#     length (start→end vertex), not per-face — this is the best approximation
#     available without BRepFilletAPI_MakeFilletFaceSet (which lacks JS bindings).
#     UI: filletDialog already has offsetOne/offsetTwo fields in useFilletDialogState.
